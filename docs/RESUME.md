# Start a fresh SatQuery session here

Snapshot date: 2026-09-07. This file is a handoff, not live repository truth.
Fetch current main, PR heads and Git status before acting.

## Goal
Aryan is a beginner technical lead preparing tomorrow's college SIH internal demo.
Eight minutes for presentation/demo plus two minutes Q&A. Need clear instructions,
parallel UI/backend delivery, polished independently removable landing and map workspace.
Respect the mandatory R1-R8 in PROJECT_CONTEXT, including optical/SAR/temporal,
actual visual adaptation (no new LoRA), agentic execution, evidence and exports.
No fake scientific results, confidence, provenance, benchmarks or live progress.

## Verified checkpoint
PR1 test discovery, PR2 locked Python3.12, PR3 Ruff merged.
Inspected main SHA: 83f96cea2394d61ee86433e6415aef6b2b30c1d6.
PR4 mypy at 629d26315804ce7d864bd673c59791945c65a333 reviewed without a blocking
regression; GitHub checks pass; awaiting user merge as of snapshot. It changes inferred
report-route OpenAPI, so do not say unchanged schemas/ proves unchanged HTTP schemas.
72 collected tests (42 unittest included), locked environment, Ruff and mypy are
development checks, not evidence the scientific product works.

No apps frontend exists in inspected main. Scripted specialists still marked real,
queued-only HTTP flow, empty claims, incomplete payload/report retrieval and ownership
are known application gaps. Next backend: FIX-02 truthful implementation modes,
then establish an actual pixel-consuming model and end-to-end API slice.
Read DEMO_EXECUTION_PLAN for scope/acceptance; do not restart generic research.

## Approved decisions
- apps/web workbench; apps/landing optional independently built landing.
- Intended combined-host base /app/ for workspace; direct dev workspace URL sufficient.
- Existing UI five routes remain relative to workspace base; no marketing hero there.
- Progress from real events; mock and recorded runs distinctly labelled.
- Sol owns Python/API/generated client/root config; Muse owns UI in sanitized isolation.
- No concurrent agents editing one working directory; one branch and handoff per lane.
- Sol imports frontend only after Muse stops editing; shared STATUS is integrator-owned.
- App-local frontend manifests/locks explicitly assigned to UI; no root workspace change.
- Existing trusted WSL runtime stays for demo; GitHub is the shared source of truth.
- Default bounded implementation uses Sol; UI uses Muse Contributor only on approved
public/synthetic inputs with enforced isolation or chat-only generation.

## Reading order
AGENTS.md; this file; PROJECT_CONTEXT.md; ARCHITECTURE.md; your lane in
DEMO_EXECUTION_PLAN.md; UI_SPEC and prompts/UI_WORKER for frontend; exact assigned
ticket/repair and latest lane handoff. README/older ticket progress text contains
historical starter statements; verify code and latest reviewed evidence.
Do not load the 438-page research history or all old chats.

## Handoff required after each subtask
Worker writes only its assigned docs/handoffs/<lane>.md:
- Task, owner, writable file paths, branch, base SHA and latest commit.
- What actually changed and what remains mock/unavailable.
- Commands run, exit results, test evidence, unrun checks.
- PR URL, CI head/result, uncommitted/untracked files.
- Startup commands, data/model assumptions, environment destination (no secrets).
- Known failures, immediate next action and decisions needed.
Push coherent checkpoints when authorized; unpushed work is invisible on GitHub.

A new session must restate goal, assigned paths, current SHA, checks and next action
before editing. If handoff and code differ, investigate the difference; never fabricate
missing context. Start fresh at a task/PR boundary instead of waiting for exhaustion.
Context usage and five-hour account quota are different limits. Do not invent either
percentage. In clients exposing context/status commands use the actual displayed values.

## Progress reporting
Use the 8-checkpoint demo rubric in DEMO_EXECUTION_PLAN, with evidence links.
Initial evidence: 1/8 = 12.5% (engineering gate on reviewed PR4; merge still pending).
This is demo acceptance coverage, not percentage of code written, probability of winning,
or production readiness. Update after actual new verified capability, not each commit.
